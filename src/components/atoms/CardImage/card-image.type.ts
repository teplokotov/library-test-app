/**
 * .
 * @prop {string} link - Ссылка на изображение. Необязательное свойство.
 * @prop {string} title - Название изображения. Необязательное свойство.
 */

export type CardImageProps = {
    link?: string;
    title?: string;
}
